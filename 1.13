# Tarea-1-de-IPOO

#include <stdio.h>

main()
{
    int c;

    while ((c=getchar())!=EOF)
    {
        if (c=='\n' || c=='\t' || c==' ')
            putchar('\n');
        else
            putchar('*');
    }
}
